


class ProcessQueue(object):
	def __init__(self, url):
		self.urls = []
		if url not is None

	def add_url(self,url):
		if url not is None:
			self.urls.append(url)

	def process(self):
		# get date if url is already in es
	


		